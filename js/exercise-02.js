/* 
    1. Declare a variable using let with any name you like, but without a value.
    2. On a separate line, assign the variable the String: "Moo!"
    3. Log out this variable to the terminal using console.log
    
    Repeat the above steps but try using a const variable. Comment this new part of code and explain what is happening.
*/

let Georgina;

Georgina = "Moo!";
console.log(Georgina);

const cat = "Moo!";
console.log(cat);

/* This new part of code declares a const variable that assigns the string in the same line at the same time 
    unlike the let code that first declares and then assigns. */
// The difference between let and const is that let can be reassigned and const cannot be reassigned.
